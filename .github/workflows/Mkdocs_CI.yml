name: Mkdocs CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: 'Deploy MkDocs with customed awesome plugin'
      run:
        using: 'docker'
        image: 'Dockerfile'
